================================================================================
テスト設計書 レビュー結果報告書
================================================================================

対象文書 : docs/Test_Design/test_design_p3_v0.4.md（1,065行・v0.3比 +47行）
レビュー日: 2026-09-06
レビュアー: Claude（クリティカルシンキング・レビュー担当）
前回報告書: review_test_design_p3_v0.3.txt（🔴0件／🟡2件／🟢1件）
関連報告書: review_detail_design_p3_v0.3.txt（同時レビュー・本書と相互参照）

--------------------------------------------------------------------------------
0. 総合判定
--------------------------------------------------------------------------------

  ■ 前回指摘の解消

    TD2-01（🟡）・TD2-02（🟡）・TD2-03（🟢）── **全3件解消**。
    TD2-01 は当方が推奨した案B（`import main` して `app.user_middleware` を
    検査する）が **TC-05-11** として実装され、
    **3版にわたって残っていた §1-2-2 の穴が塞がった**。

  ■ 本版の指摘件数

    🔴 致命的 : 0件
    🟡 要改善 : 0件
    🟢 軽微   : 3件

  ■ 総評

    **本書はテスト実装に進める状態にある。**

    TD2-01 への対応は、当方の推奨をそのまま採るのではなく
    **一次情報で裏を取ってから採用している**点が良い。

      「**`Middleware.kwargs` の実在を確認した**（2026-09-06・
        fastapi 0.138.1 / starlette 1.3.1）。`app.user_middleware[N]` は
        `cls` / `args` / `kwargs` を持つ。**版に依存する属性のため、
        手順8で実装する際に再確認する**」

    そのうえで §9 #15 に「TC-05-11 の実装時に `Middleware.kwargs` の
    属性名を再確認する。**Starlette の内部構造に依存する**ため版で変わりうる」
    を引き継いでいる。**外部ライブラリの内部構造に触れるテストを書くという
    判断のリスクを、書いた本人が明示している**。

    TD2-02 への対応も、`LoginForm.jsx` を移動して終わりにせず、
    §8-2 に「**⚠️ v0.3 は本項目を新設した版で、除外行の全件に当てていなかった。
    項目を新設したその回に対象を先に列挙してから当てるべきだった**」と
    自己批判を残している。これは当方が同時に自分の観点表へ入れた規則
    （§5-3-2）と同じ内容であり、**レビュー側と開発側が同じ失敗から
    同じ規則を独立に導いた**形になっている。

    残る🟢3件は、いずれも「結論は正しいが、その理由を述べた記述が
    古いまま／書かれていない」類である。テストの内容には影響しない。

--------------------------------------------------------------------------------
1. 自己検出漏れの記録（観点表§5-3・必須）
--------------------------------------------------------------------------------

  【本版の自己検出漏れ率：3件中2件（67%）】（前回33%・その前100%）

  | 今回の指摘 | 前回検出できたか | 理由 |
  |-----------|----------------|------|
  | TD3-01 | ❌ **当方の推奨が誤りを招いた** |
  |        | 前回 TD2-01 の対処案で、当方は案Bについて |
  |        | 「**案Bを採る場合、§1-2-2 の記述はそのままで根拠が成立する**」 |
  |        | と書いた。しかし成立するのは**結論**（対象に含めてよい）だけで、 |
  |        | §1-2-2 が述べる**機構**（「`TestClient` で検証できる」）は |
  |        | 成立しない。TC-05-11 は **`TestClient` を使わない**からである。 |
  |        | 開発側は当方の言葉どおり §1-2-2 を据え置いた。**指摘した側の |
  |        | 詰めが甘く、直すべき箇所を「直さなくてよい」と言った。** |
  | TD3-02 | ❌ **2版連続で検出可能だった** |
  |        | 当方は v0.2・v0.3 の両方で §1-2-1 に 2-8（一覧表の網羅性）を |
  |        | 当てたが、**「要望1採用時に加わるファイル」という軸を |
  |        | 一度も見ていない**。`db/models.py`（§3-4）と |
  |        | `run_batch_once.py`（新規・T-P3-15 の対象）は |
  |        | 採否確定後に改修部分へ加わるが、§1-2-1 に記述がない。 |
  |        | 当方は**採否非依存のファイルだけを列挙して照合していた**。 |
  | TD3-03 | ⏭️ 対象外（v0.3 で詳細設計書側に新設された項目との比較であり、 |
  |        | v0.3 レビュー時点では詳細設計書 v0.3 が存在しなかった） |

  ■ 反省：「そのままでよい」と言うときこそ文言まで見る

    TD3-01 は、当方が**能動的に「直さなくてよい」と述べた**ことで生じた。
    指摘を書くときは「何を直すか」に注意が向くが、
    **「直さなくてよい」と述べる箇所は検証が甘くなる**。
    §1-2-2 の文を読み直せば「`TestClient` で検証できる」と書いてあり、
    当方が推した案Bは `TestClient` を使わないのだから、
    その場で気付けたはずである。

  ■ 反省：一覧の網羅性は「軸」を先に決める

    TD3-02 は、2-8 の当て方が浅かったことによる。
    当方は「詳細設計書が変更するファイル」を列挙して照合したが、
    その列挙自体が**採否非依存のものに偏っていた**。
    本書は §0-3 で「採否依存の扱い」を明示的に節にしているのだから、
    **一覧を照合するときも採否の軸で2回当てる**べきだった。

  ■ 観点表への反映

    docs/review_checklist.md §5-3 に以下を追加した。

      5-3-3 **「この記述はそのままでよい」と述べるときは、
              結論だけでなく文言まで検証してから言う。**
              対処案の中で「据え置き可」と書いた箇所は、
              指摘した箇所と同じ強度で読む。
              （失敗例：TD2-01 の案Bで「§1-2-2 はそのままで根拠が成立する」と
                書いたが、§1-2-2 は機構を「`TestClient` で検証できる」と
                述べており、案B（`import main`）は `TestClient` を使わない）

      5-3-4 **一覧表の網羅性（2-8）を当てるときは、
              先に「照合の軸」を列挙する。**
              条件つきの一覧（採否・環境・工程で対象が変わるもの）は、
              **条件ごとに1回ずつ当てる**。
              （失敗例：§1-2-1「改修部分」に 2-8 を2版連続で当てながら、
                「要望1採用時に加わるファイル」の軸を一度も見ていなかった）

--------------------------------------------------------------------------------
2. 前回指摘（v0.3）の解消確認
--------------------------------------------------------------------------------

  | ID     | 重要度 | 指摘内容 | 解消 | 確認方法 |
  |--------|-------|---------|------|---------|
  | TD2-01 | 🟡    | §1-2-2 の根拠が成立しない（`main.py` L33-39 が実行されない） | ✅ | 実測＋本文 |
  | TD2-02 | 🟡    | `LoginForm.jsx` が改修部分に無く除外行にある | ✅ | 本文 |
  | TD2-03 | 🟢    | 自己チェック・追跡表の記述が古い（3件） | ✅ | 再計数 |

  ■ TD2-01 の解消内容 ── TC-05-11 の新設

    当方が推奨した案B が **TC-05-11** として実装された。

      def test_main_cors_options_match_config():
          import main
          from config import CORS_ALLOW_HEADERS, CORS_ALLOW_METHODS
          mw = next(m for m in main.app.user_middleware
                    if m.cls.__name__ == "CORSMiddleware")
          assert mw.kwargs["allow_methods"] == CORS_ALLOW_METHODS
          assert mw.kwargs["allow_headers"] == CORS_ALLOW_HEADERS

    **当方でも実際に動作を確認した**（2026-09-06・fastapi 0.141.1 /
    starlette 1.6.0）。

      >>> app.add_middleware(CORSMiddleware, allow_origins=['x'],
      ...     allow_methods=['GET','POST','PUT'], allow_headers=['Authorization'])
      >>> mw = next(m for m in app.user_middleware
      ...           if m.cls.__name__=='CORSMiddleware')
      >>> mw.kwargs
      {'allow_origins': ['x'], 'allow_methods': ['GET','POST','PUT'],
       'allow_headers': ['Authorization']}

    → **`kwargs` に CORS のオプションがそのまま入る。TC-05-11 は成立する。** ✅

    本書が新設した注記の主張も1件ずつ確認した。

    | 主張 | 検証 | 結果 |
    |------|------|------|
    | 「`main.py` が `["*"]` のままでも TC-05-1〜10 は全部通る」 | フィクスチャが定数を読む以上、`main.py` の値は結果に影響しない | ✅正しい |
    | 「`import main` が安全（`scheduler.py` L252-254 に `start()` が無い）」 | 実物確認：L252 `BackgroundScheduler()` / L253-254 `add_job` / ファイルは254行で以降なし。`start()` は `main.py` L56 | ✅正しい |
    | 「案B を却下したのは `TestClient(main.app)` を使う案であって `import main` そのものではない」 | 詳細設計書§2-4-1 の案B は「フィクスチャを `main.py` の `app` を import して作る」＝`TestClient` 前提 | ✅正しい |
    | 「`Middleware.kwargs` は版に依存する属性」 | Starlette の内部構造。公開APIではない | ✅正しい。§9 #15 に引き継ぎ済み |

    照合リストに **[19]** が追加され、`scheduler.py` L252-254 と
    `main.py` L56 が **TC-05-11 の前提を裏づけるため**に引用されている。
    「**テストの前提になっている実装上の事実も照合対象である**」という
    運用であり、正しい ✅

  ■ TD2-02 の解消内容

    | 対処 | 反映先 | 確認 |
    |------|-------|------|
    | 改修部分に `components/LoginForm.jsx` を追加 | §1-2-1 JavaScript 行 | ✅ |
    | 除外行から `LoginForm.jsx` を削除 | §1-2-1 除外（JavaScript）行 | ✅ |
    | 除外行の各ファイルを1件ずつ検証した結果を記録 | §8-2 | ✅ |

    除外行は `main.jsx`・`CategoryTabs.jsx`・`GameSchedule.jsx`・
    `SpoilerOverlay.jsx` の4件になった。
    §8-2 に各ファイルの「変更されるか／根拠」の表が置かれ、
    `LoginForm.jsx` の行には
    「詳細設計書§4-2-1（429 の分岐）・本書 T-P3-14 TC-14-3・
      §7 マトリクス F-17 行・詳細設計書§6 #5」と
    **4つの根拠**が並べられている。当方が挙げた4件と一致する ✅

  ■ TD2-03 の解消内容 ── 3件すべて

    | # | v0.3 | v0.4 | 確認 |
    |---|------|------|------|
    | 1 | §8-1「テストID（T-P3-01〜16）」 | **「T-P3-01〜17」** | ✅（実在は01〜17） |
    | 2 | §0-2「11件すべてを追跡する」 | **「詳細設計書 v0.3 §6 の14件すべてを追跡する」** | ✅（詳細設計書§6 は14件） |
    | 3 | §8-1「詳細設計書§6 の11件」 | **「詳細設計書 v0.3 §6 の14件」** | ✅ |

    当方が推奨した「**上位文書の版を明記する**」も反映されている
    （「詳細設計書 **v0.3** §6 の14件」）。
    次に上流が改版されたとき、件数のずれが目に付きやすくなる ✅

    §0-2 に追加された3行の扱いも妥当である。

      | #12 | DD-03（基本設計書への是正） | ⏭️**本書の対象外**（基本設計書側） |
      | #13 | `constants.js` の作成 | ✅**§9 #14 に記載** |
      | #14 | T-P3-05 のフィクスチャを案Bへ | ✅**TC-05-11 により当面は移行不要** |

    #14 について「**TC-05-11（v0.4）により当面は移行不要**」と
    **判断を書き添えている**点が良い。⏭️のまま放置していない。

--------------------------------------------------------------------------------
3. 本版の指摘（🟢3件）
--------------------------------------------------------------------------------

  ────────────────────────────────────────────────────────────────────────────
  🟢 TD3-01：§1-2-2 が述べる「`TestClient` で検証できる」という機構が、
             実際に成立させている TC-05-11 と食い違う
  ────────────────────────────────────────────────────────────────────────────

  【該当】§1-2-2 の注記（L123・v0.2 から変更なし）

    「**`main.py` の扱い**: `startup_event()` / `shutdown_event()` /
      `uvicorn.run()` は**除外を維持**する（プロセス起動を伴い単体テストに
      向かない）。**CORS設定（L33-39）は `TestClient` で検証できる**ため
      対象に含める（T-P3-05）。」

  【事実】§1-2-2 の**結論は正しくなったが、理由が間違っている**。

    | | v0.3 まで | v0.4 |
    |---|---------|------|
    | 結論（`main.py` を対象に含める） | ❌根拠が無かった | ✅**TC-05-11 により成立** |
    | 機構（`TestClient` で検証できる） | ❌成立しない | ❌**依然として成立しない** |

    L33-39 を実行するのは **TC-05-11** であり、
    本書自身が T-P3-05 の注記で次のように書いている。

      「**`import main` して `app.user_middleware` を検査**
        （`TestClient` を使わない）」
      「**TC-05-11 は `TestClient` を使わないため、`startup_event()` は
        走らない**」

    一方 `cors_client` フィクスチャ（`TestClient` を使う）は
    **`main.py` を import せず**、自前で `FastAPI()` を組む。
    したがって `TestClient` を使うテスト（TC-05-1〜10）は
    **`main.py` L33-39 を1行も実行しない**。

    → **「`TestClient` で検証できる」は、v0.4 でもなお誤りである。**

  【問題】

    §1-2-2 は「`main.py` のカバレッジ除外を見直す」という
    **方針変更の根拠を述べる節**である。
    §1-2-2 だけを読んだ人は
    「`TestClient` を使えば `main.py` の CORS 行が通る」と理解する。
    実際にはそうではなく、**`import main` という別の手段が必要**である。

    手順8で `--cov` の設定を組む人が
    「TC-05-1〜10 があるから `main.py` はカバーされるはず」と考え、
    **TC-05-11 を落とすと L33-39 は 0% に戻る**。
    TC-05-11 は T-P3-05 の11番目のケースであり、
    他の10件と違って**フィクスチャを使わない特殊なケース**であるため、
    実装時に取りこぼされやすい。

  【重要度が🟢である理由】

    TC-05-11 は既に設計されており、§7 マトリクス・§9 #15 にも紐づいている。
    検証の内容としては穴がない。誤っているのは説明文1文である。

  【当方の責任】

    前回 TD2-01 の対処案で、当方は案Bについて
    「**案Bを採る場合、§1-2-2 の記述はそのままで根拠が成立する**」と書いた。
    **これが誤りである。** 成立するのは結論だけで、機構は成立しない。
    開発側は当方の言葉どおり据え置いた。指摘した側の詰めの甘さである。

  【対処】§1-2-2 の注記を次に差し替える。

    「**`main.py` の扱い**: `startup_event()` / `shutdown_event()` /
      `uvicorn.run()` は**除外を維持**する（プロセス起動を伴い単体テストに
      向かない）。**CORS設定（L33-39）は TC-05-11（`import main` して
      `app.user_middleware` を検査する）で実行される**ため対象に含める。
      **`TestClient` を使う TC-05-1〜10 は `main.py` を import しない**
      （`cors_client` が自前でアプリを組むため）ので、
      **TC-05-11 を落とすと `main.py` は 0% に戻る**。」

    あわせて §1-2-2 の表の理由欄も
    「除外したままだと F-21 が1行もテストされない」→
    「**除外したままだと TC-05-11 の実行結果が数値に現れない**」と
    正確にできる（任意）。

  ────────────────────────────────────────────────────────────────────────────
  🟢 TD3-02：§1-2-1「改修部分」の定義に、要望1採用時に加わるファイル
             （`db/models.py`・`run_batch_once.py`）の扱いが書かれていない
  ────────────────────────────────────────────────────────────────────────────

  【該当】§1-2-1「改修部分」の定義

  【事実】§1-2-1 の Python 側は次の7ファイルを挙げる。

      `api/routes.py`・`api/limiter.py`（新規）・`db/crud.py`・
      `auth/jwt.py`・`auth/users.py`・`processor/claude_client.py`・
      `main.py`（CORS設定 L33-39 のみ）

    一方、詳細設計書 v0.3 §3（要望1採用時）は次を変更・新規作成する。

      | ファイル | 詳細設計書 | 本書のテスト |
      |---------|-----------|------------|
      | **`backend/run_batch_once.py`**（**新規**） | §3-1 | **T-P3-15**（TC-15-1〜7） |
      | **`db/models.py`**（接続URL化） | §3-4 | — |
      | `config.py`（用途別分離） | §3-2 | T-P3-07（**採否非依存**） |
      | `.github/workflows/batch.yml`（新規） | §3-3 | — |
      | `api/routes.py`（`/api/fetch` 削除） | §3-5 | §6-5 |

    → **`run_batch_once.py` と `db/models.py` が §1-2-1 に現れない。**
      §1-2-1 には「要望1採用時は別扱い」といった但し書きもない。

  【問題】

    「改修部分80%以上」は**合否判定**である（§1-2）。
    工程順は **手順6（採否）→ 手順8（実装）** であり、
    **手順8の時点では採否が確定している**。
    要望1が採用された場合、`run_batch_once.py` は
    **本フェーズで新規に書かれる本番コード**（`init_db()` → バッチ2種 →
    成否判定 → ping）であるにもかかわらず、
    **合否判定の分母に入らない**。

    T-P3-15 が7ケース設計されているため、実際にはテストは書かれる。
    しかし**「80%以上」という要求が掛からない**状態になる。
    分母の定義が「採否非依存のファイルのみ」であることが
    どこにも書かれていないため、
    手順8で `--cov` の対象を組む人が判断できない。

  【重要度が🟢である理由】

    現時点（採否未確定）では実害がない。§6 が
    「テストコードを作成しない・実施は手順6の後」としているため、
    いま分母に入れる必要もない。
    **要望1の採用が確定した時点で🟡相当になる**性質の指摘である。

  【対処案】

    案A（推奨）: §1-2-1 に**条件つきの行を1つ足す**。

      | 系統 | 対象ファイル |
      |------|------------|
      | Python（採否非依存） | （現行7件） |
      | **Python（要望1採用時に追加）** 🆕 | **`run_batch_once.py`（新規）**・**`db/models.py`（§3-4）** |
      | JavaScript | （現行10件） |
      | 除外（Python） | `config.py`・`main.py` の startup／shutdown／`uvicorn.run` |
      | 除外（JavaScript） | `main.jsx`・`CategoryTabs.jsx`・`GameSchedule.jsx`・`SpoilerOverlay.jsx` |

      あわせて「**`batch.yml` はワークフロー定義でありカバレッジの対象外**
      （実行結果は手順9の実機確認で見る）」を1行添える。

    案B        : §6 の冒頭に「要望1採用時は §1-2-1 の改修部分に
                 `run_batch_once.py`・`db/models.py` が加わる」と書く。
                 → 分母の定義が2箇所に分かれる。案Aの方が良い。

  ────────────────────────────────────────────────────────────────────────────
  🟢 TD3-03：TD2-03 と同型（表に行を足したあと、それを指す記述を追っていない）
             への再発防止項目が §8-2 に無い
  ────────────────────────────────────────────────────────────────────────────

  【該当】§8-2（B：判断）

  【事実】v0.4 は前回指摘3件のうち2件について**再発防止項目を持っている**。

      | 指摘 | 対応する §8-2 の項目 |
      |------|--------------------|
      | TD2-01 | 「回帰テストが本番の値・本番のコードを読んでいるか」（v0.3新設） |
      | TD2-02 | 「『対象外』と宣言したものを、対象外でよいか検証したか」（v0.3新設・**v0.4で全件適用**） |
      | **TD2-03** | **該当項目なし** |

    一方、**詳細設計書 v0.3 は同型の指摘（DD2-02／DD2-03）に対して
    §5-2 に項目を新設している**。

      「**表に行を足したあと、その表を指す記述を追ったか** 🆕（v0.3追加）
        → v0.2 は §6 に3行（#12〜#14）を足したが§2-4-1 の番号参照が
          #12 のままだった。§1-7 を新設したが§5-1 の参照先ファイル一覧に
          反映していなかった。**同じ型が2件同時に出た**」

    TD2-03 は**まさにこの型**である（T-P3-17 を足したが §8-1 の
    「T-P3-01〜16」を追っていない／詳細設計書§6 が14件になったが
    §0-2 が11件のまま）。

  【問題】

    本プロジェクトの品質プロセスは、
    **指摘を個別に直すのではなく自己チェック項目に翻訳して再発を防ぐ**
    という運用で成り立っている（計画書§8-0-3）。
    TD2-03 だけがその翻訳を経ていない。

    実害は小さい。ただし本書は今後も
    §0-2（上位文書の追跡）・§7（マトリクス）・§9（引き継ぎ）という
    **番号で相互参照する表を3つ**持ち続ける。
    T-P3-18 を足す場面は今後も起こりうる。

  【重要度が🟢である理由】

    提案寄りの指摘である。現時点の記述に誤りはない。

  【対処】§8-2 に1項目を追加する（詳細設計書§5-2 と同じ文面でよい）。

    「**表に行を足したあと、その表を指す記述を追ったか**
      → v0.3 は §3-8（T-P3-17）を新設したが、
        **§8-1 の『T-P3-01〜16』と §0-2 の『11件』を追っていなかった**。
        本書は §0-2・§7・§8-1・§9 の4つの表が番号で相互参照するため、
        1つに行を足したら残りを全件見る。」

  【あわせて】詳細設計書レビュー DD3-01 で、
    詳細設計書§5-2 の同項目の対象を「**表・コードブロック**」に
    広げることを提案した。本書に追加する際も同じ範囲にすると、
    両文書で運用が揃う。

--------------------------------------------------------------------------------
4. 検証したが指摘に至らなかった事項（記録）
--------------------------------------------------------------------------------

  ■ 4-1. TC-05-11 が依拠する `Middleware.kwargs`

    【疑義】Starlette の内部構造に依存するテストである。
            属性名が実在するか、CORS のオプションが実際に `kwargs` に
            入るかを、訓練データの知識で判断してはならない。

    【検証】サンドボックスで実測した（2026-09-06）。

        fastapi 0.141.1 / starlette 1.6.0
        >>> app.add_middleware(CORSMiddleware, allow_origins=['x'],
        ...     allow_methods=['GET','POST','PUT'],
        ...     allow_headers=['Authorization'])
        >>> mw = next(m for m in app.user_middleware
        ...           if m.cls.__name__=='CORSMiddleware')
        >>> hasattr(mw,'kwargs'), hasattr(mw,'args'), mw.cls.__name__
        (True, True, 'CORSMiddleware')
        >>> mw.kwargs
        {'allow_origins': ['x'], 'allow_methods': ['GET','POST','PUT'],
         'allow_headers': ['Authorization']}

    【結論】**TC-05-11 は成立する。指摘なし。**

            なお本書が記載した版（fastapi 0.138.1 / starlette 1.3.1）は
            当方の確認時点（0.141.1 / 1.6.0）より古い。
            **`requirements.txt` は `fastapi` を版無指定にしている**ため、
            手順8の時点でさらに新しい版が入る可能性がある。
            §9 #15（実装時に属性名を再確認）が正しく置かれている ✅

            **補足**: `mw.kwargs` には `add_middleware()` に渡した
            キーワード引数だけが入る。`main.py` は
            `allow_origins` / `allow_credentials` / `allow_methods` /
            `allow_headers` の4つを渡すため、
            TC-05-11 が検証する2つ以外も取得できる。
            **`allow_origins` は環境変数由来のため定数と比較できない**が、
            `allow_credentials=True` は加えてもよい（任意）。

  ■ 4-2. `import main` の副作用（TC-05-11 の前提）

    【検証】前回（v0.3 レビュー §4-1）で確認済み。本版で再確認した。

        $ wc -l backend/scheduler.py → **254**
        $ sed -n '252,254p' backend/scheduler.py
          scheduler = BackgroundScheduler()
          scheduler.add_job(check_and_run_batch, "interval", ...)
          scheduler.add_job(check_and_run_game_schedule_batch, "interval", ...)
        $ sed -n '56p' backend/main.py →     scheduler.start()

    【結論】**`import main` はスケジューラを起動しない。指摘なし。**
            本書の照合リスト [19] が
            「**ファイル全体が254行で以降なし**」まで記録している点が良い。
            「L252-254 に `start()` が無い」だけでは
            「L255 以降にあるかもしれない」が残るが、
            **行数を添えることで否定が完結している**。

  ■ 4-3. 2-8（一覧表の網羅性）の全件適用 ── 軸を2つ立てて当てた

    【方法】前回の反省（§1）を踏まえ、**採否の軸で2回**当てた。

    【軸1：採否非依存】詳細設計書 v0.3 §1〜§2 が変更する全ファイル

      フロント: useNews.js ✅ / levenshtein.js ✅ / relatedness.js ✅ /
                constants.js ✅ / NewsCard.jsx ✅ / RelatedNews.jsx ✅ /
                FilterBar.jsx ✅ / App.jsx ✅ / useAuth.js ✅ /
                **LoginForm.jsx ✅**（v0.4 で追加）
      バック  : routes.py ✅ / limiter.py ✅ / crud.py ✅ / jwt.py ✅ /
                users.py ✅ / claude_client.py ✅ / main.py ✅ /
                config.py ✅（除外・妥当）/ requirements.txt（—・妥当）
      → **欠落なし** ✅

    【軸2：要望1採用時】詳細設計書 v0.3 §3 が変更・新規作成するファイル

      `run_batch_once.py`（新規）❌ / `db/models.py` ❌ /
      `config.py`（§3-2・採否非依存）✅ / `batch.yml`（新規）❌ /
      `api/routes.py`（§3-5）✅（軸1で既出）
      → **`run_batch_once.py`・`db/models.py` が §1-2-1 に無い** → **TD3-02**
        （`batch.yml` はワークフロー定義でカバレッジ対象外。ただし
          その旨も書かれていない）

    【結論】**軸を2つ立てたことで TD3-02 を検出できた。**
            軸1だけなら「欠落なし」で終わっていた。

  ■ 4-4. 2-9（例示コードと注記・表の整合）の全件適用

    【方法】コードブロックを機械抽出してから当てた。

    | 節 | ブロック | 注記との整合 |
    |----|---------|------------|
    | §1-3 | `vite.config.js` | ✅（`exclude` から `App.jsx` が外れ、注記と一致） |
    | §1-3 | `setupTests.js`／`package.json` | ✅ |
    | §1-6-1 | 現状の `conftest.py` 抜粋 | ✅（実物 L8-11／L19-23 と一致） |
    | §1-6-2 | 変更後の `conftest.py` | ✅（「`setdefault` ではない」の注記どおり無条件代入） |
    | §2-0 | `reset_rate_limit` | ✅（`from api.limiter import limiter` ＝詳細設計書 v0.3 §2-1-1 と一致） |
    | T-P3-05 | `cors_client` | ✅（「値を書かない」の注記どおり定数を読む） |
    | **T-P3-05** | **TC-05-11** | ✅（「`TestClient` を使わない」の注記どおり） |
    | §3-8 | `vi.mock('../hooks/useAuth')` | ✅ |

    【結論】**不整合なし。** ただし §1-2-2 の**注記**（コードブロックではない）が
            T-P3-05 の記述と食い違う → TD3-01（2-1 同格の節どうしの整合で検出）

  ■ 4-5. §0-2 と詳細設計書§6 の件数一致

    【検証】

      $ 詳細設計書 v0.3 §6 の行数 → **14**（採番1〜14）
      $ 本書 §0-2 の行数 → **14**（採番1〜14・ヘッダ除く）
      $ 本書 §0-2 の見出し → 「詳細設計書 **v0.3** §6 の14件すべてを追跡する」

    【結論】**件数・版とも一致。TD2-03 は完全に解消。指摘なし。**

  ■ 4-6. §9 の15件と引き継ぎ先の妥当性

    【検証】v0.3 の14件に **#15**（TC-05-11 実装時に `Middleware.kwargs` を
            再確認・手順8）が追加された。

    【結論】**妥当。指摘なし。**
            外部ライブラリの内部構造に依存するテストを書く判断をしたうえで、
            **その依存を引き継ぎ事項として明示している**。
            §8-2 の「版番号を本文の指示に使っていないか」と同じ思想であり、
            運用が一貫している。

--------------------------------------------------------------------------------
5. 手順5を継続する場合の作業（本書 v0.5 で反映すべきもの）
--------------------------------------------------------------------------------

  | # | 対象 | 内容 | 重要度 |
  |---|------|------|-------|
  | 1 | §1-2-2 | 注記の機構を「`TestClient` で検証できる」→「**TC-05-11（`import main`）で実行される**」に。**TC-05-11 を落とすと 0% に戻る**旨を添える | 🟢 |
  | 2 | §1-2-1 | 「Python（**要望1採用時に追加**）」の行を新設し `run_batch_once.py`・`db/models.py` を記載。`batch.yml` がカバレッジ対象外である旨も1行 | 🟢 |
  | 3 | §8-2 | 「表に行を足したあと、その表を指す記述を追ったか」を追加（詳細設計書§5-2 と同文面。対象は「表・コードブロック」） | 🟢 |

  ■ 進め方についての所見

    **本書は🔴・🟡がいずれも0件であり、テスト実装に進める状態にある。**
    上記3件はいずれも説明文・定義の精度に関するもので、
    テストケースの内容には影響しない。
    #2 は**要望1の採用が確定した時点で対応が必要**になるが、
    それは手順6の後であり、いま急ぐ必要はない。
    進め方の判断はTakumi様にお任せする。

--------------------------------------------------------------------------------
6. 観点表の適用記録（docs/review_checklist.md v1.3 §5-1）
--------------------------------------------------------------------------------

  ■ §1 機械確認

  ┌ 1-1 参照先ファイル・パスの実在 ─────────────────────────────────────────┐
  │ `test_design_p2_v0.2.md`・`manual_check_p2_v1.1.md`・                     │
  │ `basic_design_p3_v0.7.md`・**`detail_design_p3_v0.3.md`**・               │
  │ `review_test_design_p3_v0.3.txt`・`tests/conftest.py`・                   │
  │ `frontend/vite.config.js`・`frontend/package.json`・                      │
  │ `tests/test_routes.py`・`tests/test_auth.py`・`tests/test_integration.py`・│
  │ `backend/scheduler.py`・`backend/main.py`                                 │
  │ → **全件実在を確認** ✅                                                    │
  │ §0-2 が**上位文書の版を明記**するようになった（v0.4）ため、                │
  │ どの版を前提としているかが一意に定まる ✅                                  │
  └────────────────────────────────────────────────────────────────────────┘

  ┌ 1-2 本文中の数値・件数の再現 ──────────────────────────────────────────┐
  │ ・本書行数        : `wc -l` → **1,065行**（v0.3 は1,018行・+47行）         │
  │ ・§0-2 の「14件」 : 表の行数**14** → ✅一致（上流§6 も14件 → ✅一致）      │
  │ ・§9 の件数       : 表の行数**15**・採番1〜15 → ✅一致                     │
  │ ・テストID        : `grep -o` → **T-P3-01〜17 の17件** → §8-1 と ✅一致    │
  │ ・§8-1 照合リスト : `[1]`〜`[19]` の19件 → §8-1 の記述と ✅一致            │
  │ ・§7 マトリクス   : F-13〜F-21 の9行＋3行 → 全要件が最低1列に埋まる ✅     │
  │ ・§3-1 TC-08-1〜6 : Python で置換コスト2の DP を実装し全件再計算 ✅        │
  │   （0.600 / 1.000 / 0.000 / 1.000 / 0.667 / 0.615）                       │
  │ ・TC-08-1 の意図欄の素朴値 **0.429** : 1−4/7 = 0.4286 → ✅一致            │
  │ ・§3-2 TC-09-4〜7 の重み計算: 1.000 / 0.800 / 0.800 / 0.800 → ✅一致       │
  │ ・TC-09-3「ゲートがなければ0.400」: 0.2×1+0.6×0+0.2×1 = 0.400 ✅          │
  │ ・§1-2-1 の「Python 7件・JavaScript 10件・除外(JS) 4件」: 数えて一致 ✅    │
  └────────────────────────────────────────────────────────────────────────┘

  ┌ 1-3 採番の一意性・昇順・衝突 ──────────────────────────────────────────┐
  │ T-P3-01〜17・TC-nn-n（TC-05 は 1〜11）・IT-P3-01〜05・シナリオ10〜13      │
  │ いずれも連続・重複なし ✅                                                  │
  │ **TC-05-11 が TC-05-10 の次に採番されている** ✅（末尾追加・自然な運用）   │
  └────────────────────────────────────────────────────────────────────────┘

  ┌ 1-4（v1.2で拡張）コード引用の照合＋条件つき行の有効性 ────────────────────┐
  │ 新規追加された [19] を `sed`／`wc -l` で照合。                             │
  │ [19] scheduler.py L252-254  BackgroundScheduler() の生成と add_job のみ    │
  │      （**start() なし**・ファイル全体が254行で以降なし）        ✅一致    │
  │      ／ main.py L56  scheduler.start() は startup_event() の中   ✅一致    │
  │ 既存 [1]〜[18] は v0.2／v0.3 レビューで全件照合済み・本版で変更なし ✅     │
  │ ── **不一致：0件**                                                        │
  │                                                                          │
  │ **1-4 の拡張適用（条件つき・既定値依存の行）**:                            │
  │  ・[2] `setdefault` ×5 と [10] `load_dotenv(override=True)` の前後関係     │
  │    → §1-6 が正しく扱っている ✅                                            │
  │  ・§1-3 の `coverage.include` に `src/App.jsx` が一致しない件              │
  │    → v0.3 レビューで「手順7の導入時に実測して確認」と記録した事項。        │
  │      v0.4 でも `include: ['src/utils/**','src/hooks/**','src/components/**']`│
  │      のまま。**`App.jsx` は `src/` 直下**であり、いずれのパターンにも      │
  │      一致しない。§9 #7（`vite.config.js` の追加）の作業に含まれるため      │
  │      指摘には挙げないが、**導入時に `App.jsx` が計測対象に入ることを       │
  │      実測で確かめる**こと                                                 │
  └────────────────────────────────────────────────────────────────────────┘

  ┌ 1-5 旧識別子の残存 ────────────────────────────────────────────────────┐
  │ 「v0.2 は〜」「v0.3 は〜」はいずれも📖履歴的記述であり、                   │
  │ 現行の設計を述べる箇所と区別されている ✅                                  │
  │ **例外：§1-2-2 の注記は履歴ではなく現行の主張として残っている**            │
  │   → TD3-01                                                                │
  └────────────────────────────────────────────────────────────────────────┘

  ■ §2 構造確認

    2-1 宣言と実体の対応
        §0-2（14件）・§7 マトリクス・§9（15件）の各行が本文に実体を持つことを
        全行確認 ✅
        **同格の節どうしの整合**で1件検出 → **TD3-01**（§1-2-2 と T-P3-05）

    2-2 前提が変わった記述の波及先
        TC-05-11 の新設が §7・§9 #15・§0-2 #14 へ波及 ✅
        `LoginForm.jsx` の移動が §1-2-1 の除外行・§8-2 へ波及 ✅
        → **TC-05-11 の新設が §1-2-2 へ波及していない** → TD3-01

    2-3 上位文書の項目の消化
        要件定義書 F-13〜F-21（§7）は全件 ✅
        **詳細設計書 v0.3 §6 の14件を §0-2 で全件追跡**（件数・版とも一致）✅

    2-4 期限・順序の整合
        §0-3 の「設計だけ書き実施は採否確定後」と、
        T-P3-07（先-1・2026年9月まで）の並存を確認。
        **本日は2026年9月6日**であり期限内。矛盾なし ✅

    2-5 未確定事項の引き継ぎ先
        「手順7」「手順8」「手順9」「手順10」の表現をすべて §9（15件）に
        引き当て。引き継ぎ先を持たない未確定事項は**0件** ✅

    2-6 差分だけでなく全文を読む
        v0.4 の変更（TC-05-11・§1-2-1・§0-2・§8-1・§8-2・§9 #15）だけでなく、
        **v0.2／v0.3 から据え置きの §1-2-2・§1-4〜§1-6・§2〜§7 も通読** ✅
        → 据え置き節から **TD3-01**（§1-2-2）と **TD3-02**（§1-2-1）を検出

    2-8 一覧表の網羅性（実体→宣言）
        **観点表§5-3-4 に従い、採否の2軸で当てた**（§4-3）。
        軸1（採否非依存）欠落なし ✅ ／ 軸2（要望1採用時）**2件欠落** → TD3-02

    2-9 例示コードと注記・表の整合
        コードブロックを機械抽出してから全件適用（§4-4）→ **不整合なし** ✅

  ■ §3 深度確認

    3-1「Xが必要になる」
        TC-05-11 の必要性を検証。
        「`main.py` が `["*"]` のままでも TC-05-1〜10 は全部通る」は正しく、
        **他の手段では代替できない** ✅

    3-2「この構成で動く／動かない」
        ・**`Middleware.kwargs` の実在と中身** → §4-1 で実測
        ・**`import main` の副作用** → §4-2 で再確認
        ・`vi.mock('../hooks/useAuth')` で `App` を描画する方式 ✅

    3-3「候補を却下する」
        T-P3-05 の3案の却下理由を再検証。
        案B（`TestClient(main.app)`）の却下は維持されており、
        **TC-05-11 は案B とは別物**（`TestClient` を使わない）である旨が
        明記されている ✅ 論理が通っている

    3-4「この案を採用する」
        TC-05-11 の採用により**新たに決めるべきこと**を検証。
        → **生じる**。①Starlette の内部構造への依存 → §9 #15 で引き継ぎ済み ✅
          ②§1-2-2 の機構の説明が変わる → **未対応** → TD3-01

    3-5「本書が採用した判断による陳腐化」
        TC-05-11 の新設が §1-2-2 の機構の説明を陳腐化させた → TD3-01

    3-6「後工程で決める」
        §9 の15件を全件検証。
        #15（`Middleware.kwargs` の再確認・手順8）は
        **実装時にしか確かめられない**ため後工程で正しい ✅
        #13（`PUT` の実機確認・手順9）は TC-05-11 が入っても
        **実ブラウザでの動作は別**であり、残しておくのが正しい ✅

  ■ §4 外部事実（確認日 2026-09-06・一次情報）

    | 事実 | 確認方法 | 結果 |
    |------|---------|------|
    | `Middleware.kwargs` の実在と中身 | fastapi 0.141.1 / starlette 1.6.0 で実測 | ✅ CORS のオプションがそのまま入る |
    | `scheduler.py` に `start()` が無い | `grep`＋`wc -l`（254行） | ✅ |
    | `vitest` の最新版 | `npm view vitest version` | 5.0.0（本書の版表と一致 ✅） |
    | `@testing-library/react` | `npm view` | 16.3.3 / `react ^18 \|\| ^19` ✅ |

    **変動可能性**: `Middleware.kwargs` は Starlette の**公開APIではない**。
    本書が確認した版（starlette 1.3.1）より当方の確認時点（1.6.0）が新しく、
    **属性は維持されている**。§9 #15 が正しく置かれている ✅

--------------------------------------------------------------------------------
7. 本ファイルの位置づけ
--------------------------------------------------------------------------------

  本ファイルはレビュー記録であり、成果物ではない。
  本番公開前のクリーンアップ対象からは除外する
  （docs/ 配下の review_*.txt と同じ扱い）。

  本報告書と `review_detail_design_p3_v0.3.txt` は対をなす。
  **本版では両文書とも🔴・🟡が0件**であり、
  残るのはいずれも説明文・体裁に関する🟢のみである。

================================================================================
以上
================================================================================
