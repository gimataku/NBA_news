# 要件定義書（フェーズ3）

- **バージョン**: v0.1
- **ステータス**: Draft
- **作成日**: 2026-08-24
- **対象**: NBAニュース翻訳アプリ 改修（フェーズ3）
- **参照**:
  - 要件定義書 v0.12（`requirements_p2_v0.12.md`）※フェーズ2最終版
  - プロジェクト計画書 v0.7（`project_plan_p3_v0.7.md`）
  - 開発振り返りレビュー（`retrospective_p2_v1.1.md`）
  - セキュリティレビュー結果（`security_review_p2_v1.0.txt`）
  - 改修要望整理（`要望整理.txt`）

---

## 1. 本書の範囲

### 1-1. フェーズ3の要件区分

| # | 要望 | 本書での扱い |
|---|---|---|
| 1 | Render有料環境から無料環境への移行 | **本書対象外**。手順2-A・2-Dのリスク評価完了後に定義 |
| 2 | ニュースをタイトルのみ表示し、クリックで本文展開 | ✅ 本書で定義（F-13） |
| 3 | チームフィルタのラベル変更 | ✅ 本書で定義（F-14） |
| 4 | ニュースクリック時に関連度の高いニュースを表示 | ✅ 本書で定義（F-15） |
| 5a | 監視「項目」の定義（基盤非依存） | ✅ 本書で定義（§4 運用指標） |
| 5b | 監視「方式」「通知方式」「スケジュール」 | **本書対象外**。手順2-A・2-Cの実証完了後に定義 |
| 6 | セキュリティレビュー指摘の対応要否判断 | ✅ 本書で定義（§5）。ただし**YEL-01は工程外の先行対応**のため対象外、**YEL-04は手順2-A完了後に判断** |

> **本書対象外とした根拠（計画書§手順1 注記1・注記2）**: 要望1と5bは、①ホスティング基盤（要望1の可否＝手順2-A）と②到達可能性の実証（手順2-C）に依存する。**手順1で確定させると手順2の結果次第で要件定義のやり直しが発生する**ため、手順2完了後に定義する。

### 1-2. 新規機能要件ID

フェーズ2の採番（F-01〜F-12）に続けて **F-13以降** を割り当てる。

| ID | 名称 | 対応する要望 | 優先度 | 区分 |
|---|---|---|---|---|
| F-13 | ニュース本文の展開表示 | 要望2 | 中 | 追加 |
| F-14 | チームフィルタのラベル変更 | 要望3 | 低 | 変更 |
| F-15 | 関連ニュース表示 | 要望4 | 中 | 追加 |

---

## 2. 機能要件の詳細

### F-13：ニュース本文の展開表示

#### 2-1-1. 仕様

| # | 項目 | 内容 |
|---|---|---|
| 1 | 初期表示 | 記事カードは**カテゴリバッジ・タイトル（`title_ja`）・メタ情報（ソース／経過時間／原文リンク）のみ**を表示する |
| 2 | 展開操作 | **タイトルをクリック**すると `summary_ja` が展開表示される |
| 3 | 折りたたみ | 展開中のタイトルを再度クリックすると折りたたまれる |
| 4 | 展開状態の保持 | **メモリ上のみ**（`useState`）。ページリロードで全件が初期状態（折りたたみ）に戻る |
| 5 | 複数展開 | 同時に複数の記事を展開できる（アコーディオンの排他制御はしない） |
| 6 | 展開インジケータ | タイトル行に展開／折りたたみを示す視覚的な手がかりを置く |

> **項目4の根拠**: フェーズ2でJWTトークンをメモリ保持（`useState`）とした方針（詳細設計書 D-06）と揃える。展開状態は永続化する価値が低く、localStorage を使わない方針も維持できる。

#### 2-1-2. F-05／F-05b（ネタバレ防止）との関係 ⚠️ 本フェーズの主要な設計論点

**採用方針：二段展開（F-05・F-05bを存続させる）** ✅

| 案 | 内容 | 採否 |
|---|---|---|
| **二段展開** | クリック展開後も `has_score=true` の記事はぼかし表示。「スコアを表示」を押して初めて内容が見える | ✅**採用**（Takumi様2026-08-24決定） |
| 展開に吸収・F-05廃止 | タイトルのみ表示でネタバレは防げるとし、ぼかし表示とF-05bを廃止 | ❌不採用 |
| 記事ごとに切り替え | `has_score=true` のみ展開後もぼかす（実質二段展開の限定版） | ❌不採用 |

**採用根拠**: 一覧画面で誤ってスコアを見てしまうリスクが最も低い。「展開に吸収」案は、**展開した瞬間にスコアが目に入る**ため、ネタバレ防止の目的（`requirements_p2_v0.12.md` §3-3）を満たさない。操作が2回に増える点は、`has_score=true` の記事に限られるため許容する。

**状態遷移**

```
[折りたたみ]  ── タイトルclick ──▶  [展開・ぼかし]  ── 「スコアを表示」click ──▶  [展開・表示]
     ▲                                    │  ▲                                          │
     └──────── タイトルclick ─────────────┘  └────────── 「スコアを隠す」click ─────────┘
```

| # | 確認事項 | 仕様 |
|---|---|---|
| 1 | `has_score=false` の記事 | 展開すると `summary_ja` がそのまま表示される（二段目なし） |
| 2 | `has_score=true` かつネタバレ防止ON | 展開するとぼかし表示。F-05bの「スコアを表示／隠す」が従来通り機能する |
| 3 | `has_score=true` かつネタバレ防止OFF | 展開すると `summary_ja` がそのまま表示される |
| 4 | 展開状態でネタバレ防止をONに戻した場合 | ぼかし表示に戻る（F-05bの仕様を維持） |
| 5 | 折りたたみ時のスコア表示状態 | **折りたたむとスコア表示状態はリセットする**（再展開時は再びぼかし表示） |

> **項目5の根拠**: 折りたたみは「読み終えた／今は読まない」という操作であり、次に開くときも保護されている方が期待に沿う。

#### 2-1-3. 著作権上の確認事項

要望2は**既に翻訳・要約済みの `summary_ja` の表示制御**であり、新たな翻訳・複製を伴わない。フェーズ2でスコープアウトしたF-09（記事全文翻訳展開・著作権法第27条の翻訳権リスク）とは**別物**である。

> **手順2-Bでの確認事項**: 上記の理解に誤りがないか、著作権上の新たなリスクを生まないかを確認する。

#### 2-1-4. 制約事項（計画書§1-2継続事項#1）

**本要件の製造着手は、フェーズ2 Section C（C-1〜C-4）のクローズ後とする。**

`SpoilerOverlay` は `NewsCard.jsx` の summary 表示領域に描画されており、本要件は同一領域を改修する。フェーズ2で未検証のまま残っているネタバレUIを、検証前に改修することを避ける。

---

### F-14：チームフィルタのラベル変更

#### 2-2-1. 現状（実装確認済み）

`FilterBar.jsx` は2ボタンのトグルで構成される。

| ボタン | 現ラベル | 状態 |
|---|---|---|
| 左 | 全チーム | `spursOnly = false` |
| 右 | **チームのみ** | `spursOnly = true` |

#### 2-2-2. 変更仕様

| ボタン | 変更前 | 変更後 |
|---|---|---|
| 左 | 全チーム | **全チーム**（据え置き） |
| 右 | チームのみ | **Spursのみ** ✅ |

**採用根拠**（Takumi様2026-08-24決定）: 「チームのみ」は何のチームか不明である。「全チーム／Spursのみ」の対比で意図が明確になる。左ボタンは既に明確なため変更しない。

> **変更範囲**: `FilterBar.jsx` の表示ラベル1箇所のみ。ロジック（`spursOnly` の真偽・API呼び出し）は変更しない。

---

### F-15：関連ニュース表示

#### 2-3-1. 仕様

| # | 項目 | 内容 |
|---|---|---|
| 1 | 表示タイミング | **F-13で記事を展開したとき**（展開と同時に表示） |
| 2 | 表示位置 | 展開した `summary_ja` の**下部** |
| 3 | 表示件数 | **上位5件** ✅（Takumi様2026-08-24決定） |
| 4 | 表示内容 | 関連記事の**タイトル（`title_ja`）のみ**。クリックで当該記事の位置へ移動または展開 |
| 5 | 関連度の算出対象 | **`is_duplicate=0` の記事のみ**（重複排除済みの記事が対象） |
| 6 | 除外条件 | 自分自身を関連ニュースに含めない |
| 7 | 該当なしの場合 | 関連度が閾値未満の記事しかない場合、**関連ニュース欄自体を表示しない** |
| 8 | ネタバレ防止 | 関連ニュースは**タイトルのみ**表示のため、`summary_ja` 由来のスコア露出は発生しない。ただし `title_ja` にスコアが含まれる可能性は手順3で確認する |

#### 2-3-2. 関連度の定義（手順2-Bで方式を確定）

**本書では「何を関連とみなすか」を定義し、算出方式は手順2-Bで決定する。**

| # | 関連とみなす基準 | 優先度 |
|---|---|---|
| 1 | **同一カテゴリ**（trade_fa / draft / injury / column） | 中 |
| 2 | **タイトルの語句一致**（選手名・チーム名の共起） | 高 |
| 3 | **取得時期の近接**（`fetched_at` が近い） | 低 |

**算出方式の候補**（手順2-Bで比較・コスト評価）

| 案 | 方式 | 追加コスト | 備考 |
|---|---|---|---|
| (a) | 既存の Levenshtein 距離を流用（`dedup.py`） | なし | 重複排除の閾値80%より低い閾値（例：30〜50%）で「類似」を判定。**実装が最も軽い** |
| (b) | TF-IDF 等の語句ベース類似度 | なし（ライブラリ追加のみ） | (a)より意味的な近さを捉えやすい |
| (c) | Claude API で関連記事を判定 | **あり** | 精度は高いが、**目的2（無料化）と競合する**（計画書§1-1 優先順位） |

> **判定基準（計画書§1-1 優先順位・手順2-B）**: 単体のコスト比較ではなく「**要望1で移行した後の無料枠の中で成立するか**」で評価する。無料枠を超える方式は優先順位（安全性＞可観測性＞コスト＞機能）に従い不採用とする。**(a)を第一候補とする。**

#### 2-3-3. 算出タイミング（手順3で設計）

| 案 | 内容 | トレードオフ |
|---|---|---|
| リクエスト時 | 展開時にAPIを呼んで算出する | 実装が単純。記事数が増えると応答が遅くなる |
| バッチ時 | 記事取得バッチで事前に算出しDBへ保存 | 表示が高速。スキーマ変更が必要 |

> 手順3（基本設計）で決定する。記事数は30日保持・週50件程度のため、**リクエスト時算出でも成立する可能性が高い**。

---

## 3. 品質基準の分類 🆕（計画書§4 項目6）

品質項目を**「合否判定項目」**と**「改善バックログ項目」**に分離する。フェーズ2では両者が混在し、**判定結果と実質的な深刻度が乖離**した（振り返り3-5：選手名の表記ゆれでA-2がNG判定になったが、中止基準には全く届かなかった）。

### 3-1. 合否判定項目（満たさなければリリースしない）

| # | 項目 | 基準 |
|---|---|---|
| 1 | 翻訳の可読性 | 意味の取れない翻訳が全体の30%を超えないこと |
| 2 | カテゴリ分類精度 | 目視確認で80%以上 |
| 3 | ネタバレ防止の機能 | `has_score=true` の記事でスコアが露出しないこと |
| 4 | 認証機能 | 想定外の第三者アクセスがないこと |
| 5 | バッチ安定性 | 成功率95%以上 |
| 6 | コスト | 月次コストが試算の150%以内 |
| 7 | **改修部分のカバレッジ** | **80%以上**（計画書 手順5） |

### 3-2. 改善バックログ項目（望ましいが合否を左右しない）

| # | 項目 | 現状 | 扱い |
|---|---|---|---|
| 1 | **選手名・用語の表記ゆれ** | ヒーロー→ヘロ、ヤニス→ギアニス等 | **F-16として対応**（下記） |
| 2 | 全体カバレッジ | フェーズ2実績94% | 94%を下回らないことを努力目標とする |
| 3 | 要約の文体の統一 | 未評価 | 記録のみ |

> **分類の指針**: 「**機能する／しない**」は合否判定、「**好ましい／好ましくない**」は改善バックログとする。表記ゆれは意味が通じており機能を損なわないため後者にあたる。

---

### F-16：選手名・用語の表記統一（改善バックログ由来）

#### 3-3-1. 対応方針

**採用方針：`claude_client.py` のプロンプトに固有名詞指針を追加する** ✅（Takumi様2026-08-24決定）

| 案 | 内容 | 採否 |
|---|---|---|
| **プロンプト指針** | プロンプトに主要選手名・用語の表記例を示す | ✅**採用** |
| 辞書で後処理置換 | 翻訳後に固有名詞辞書で置換する | ❌不採用（辞書のメンテナンスコストが継続的に発生する） |
| 許容して対応しない | 改善バックログとして記録のみ | ❌不採用 |

**採用根拠**: 辞書方式は確実だが、選手の移籍・新人の加入のたびに更新が必要で、個人開発では維持が難しい。プロンプト方式は完全には防げないが、**メンテナンスが1箇所で済み、未知の選手名にも一定の効果が期待できる**。

#### 3-3-2. 対象とする表記（トライアル運用で観測された例）

| # | 英語 | AI翻訳（誤） | 一般的な表記（正） |
|---|---|---|---|
| 1 | DeRozan | デロザン | デローザン |
| 2 | Valanciunas | バランシューナス | バランチューナス |
| 3 | Tyler Herro | タイラー・ヘロ | タイラー・ヒーロー |
| 4 | Victor Wembanyama | ヴィクター・ウェンバンヤーマ | ヴィクター・ウェンバンヤマ |
| 5 | Kel'el Ware | ケル・ウェア | ケレル・ウェア |
| 6 | Maluach | マルアッチ | マルアチ |
| 7 | Giannis | ギアニス | ヤニス |
| 8 | Matisse Thybulle | マティッセ・サイブル | マティス・サイブル |
| 9 | Apron（用語） | アプロン | エプロン |

#### 3-3-3. 合否の扱い

**本要件は改善バックログ項目であり、未達でもリリースを妨げない。** 週次チェックリストのA-2はNG判定とせず、観測値欄に表記ゆれの例を記録する運用とする。

> **コスト影響**: プロンプトへの追記は入力トークンの増加を伴う。手順2-Bで**移行後の無料枠の中で成立するか**を確認する（F-15と同じ判定基準）。

---

## 4. 運用指標の定義（改修要件5a・基盤非依存） 🆕

**「何を観測したいか」を定義する。取得方式・通知方式・スケジュールは手順2-A・2-C完了後に定義する（改修要件5b）。**

### 4-1. 観測したい指標

| # | 指標 | 目的 | 由来 |
|---|---|---|---|
| 1 | **総記事数**（`is_duplicate=0`） | DB規模の把握・無料化時の容量判断 | 振り返り3-4 |
| 2 | **直近N日の取得件数** | バッチが正常に動いているかの判断 | 振り返り3-4 |
| 3 | **カテゴリ別件数** | 分類の偏りの検知 | 振り返り3-4 |
| 4 | **最終取得日時**（`last_fetched_at`） | バッチ稼働の確認 | フェーズ2 `/api/status` 既存 |
| 5 | **API上限超過フラグ**（`api_limit_exceeded`） | 翻訳が止まっていないかの確認 | フェーズ2 `/api/status` 既存 |
| 6 | **フェールオーバー発火実績**（`is_fallback` の履歴） | **3ソース冗長化が実際に機能したかの検証** | 振り返り§4-2 #8（継続事項#6） |
| 7 | **直近24時間のERROR件数** | 異常の早期検知 | フェーズ2 Dashboard Logs で代替中 |
| 8 | **Persistent Disk 使用量** | **無料化時の移行先容量の判断根拠** | 振り返り§4-2 #9（継続事項#7） |
| 9 | **重複排除された記事数**（`is_duplicate=1`） | 重複排除ロジックが機能しているかの確認 | フェーズ2 シナリオ9で直接確認できなかった項目 |

> **指標6・8を含めた理由（計画書§1-2継続事項#6・#7）**: いずれも振り返りで「評価漏れ」として指摘された項目である。**§5に成果物として登録されないものは管理対象外になる**（振り返り§6-1）ため、運用指標として明示的に定義する。

### 4-2. 記録欄とのセット設計（振り返り3-8）

**指標は「取得手段（API）」と「記録欄（チェックリスト）」をセットで設計する。**

| 指標 | 取得手段の設計 | 記録欄 |
|---|---|---|
| 1〜3・6・9 | **ステータスAPIの拡張**（手順3・4で設計） | 週次チェックリストの観測値欄 |
| 4・5 | 既存 `/api/status` | 同上（フェーズ2から継続） |
| 7 | 手順2-Cの実証結果による | 同上 |
| 8 | 手順2-Aの「実測手段の確保」による | 同上 |

> **本項目の根拠（振り返り3-8）**: フェーズ2では3週分のコストデータがあったにもかかわらず、**記録形式の不備でトレンド分析ができなかった**。合否だけを残すと基準そのものの妥当性を後から検証できない。**取得できても記録欄がなければ意味がない。**

### 4-3. ステータスAPIの拡張方針

現行 `/api/status` は `last_fetched_at`・`source_used`・`api_limit_exceeded`・`is_fallback` のみを返す。上記の指標1〜3・6・9を追加する。

> **設計は手順3・4で行う。** 本書では「返すべき情報」のみを定義する。認証の要否・エンドポイントの分割可否も手順3で判断する。

---

## 5. セキュリティ指摘の対応要否判断

`security_review_p2_v1.0.txt` の指摘に対する判断。

### 5-1. 対象外（本書で判断しないもの）

| ID | 内容 | 理由 |
|---|---|---|
| YEL-01 | APIキー未設定でも起動する | **工程外の先行対応（先-1）**として2026年9月までに実施。計画書§1-2継続事項#4 |
| YEL-04 | `render.yaml` のバージョン未指定 | **手順2-Aの移行先確定後に判断**。移行先によっては `render.yaml` 自体が不要になる |

### 5-2. 対応要否の判断

| ID | 内容 | 判断 | 根拠・時期 |
|---|---|---|---|
| **YEL-02** | `/api/auth/login` にレートリミットがない | ✅**対応する（F-17）** | 本アプリは公開URL上にあり、JWT認証が唯一の防壁。認証は**セキュリティ目的ではなく著作権法第30条の公開範囲担保が主目的**（計画書§6制約）である以上、ブルートフォース耐性の欠如は「技術的な脆弱性」ではなく**法的前提を崩すリスク**にあたる。手順2-Dで再評価した上で手順7で実装 |
| **YEL-03** | `/api/schedule` の日付パラメータに形式バリデーションがない | ✅**対応する（F-18）** | 不正な日付での予期しない挙動を防ぐ。実装コストが小さい |
| GRN-01 | `datetime.utcnow()` が Python 3.12+ で非推奨 | ✅**対応する（F-19）** | 将来のランタイム更新で動作しなくなるリスク。`datetime.now(timezone.utc)` への置換で完結 |
| GRN-02 | `USERNAME` 環境変数名がOSの予約変数と衝突する可能性 | ⏭️**見送り** | フェーズ2の手順8で実際に問題が発生し（Windowsのシステム変数による上書き）、`conftest.py` の `.env` 先行ロードで回避済み。本番（Linux）では顕在化していない。**改修すると環境変数の再設定が必要**でリスクの方が大きい |
| GRN-03 | `NewsCard.jsx` の `source_url` が `href` に未検証で入る | ✅**対応する（F-20）** | RSSソースは信頼できるが、`javascript:` 等のスキームを弾く検証を入れる。**F-13で `NewsCard.jsx` を改修するため同時に対応できる** |
| GRN-04 | CORS `allow_methods=["*"]` / `allow_headers=["*"]` が過剰 | ✅**対応する（F-21）** | 実際に使用するメソッド（GET・POST）とヘッダに限定する。**ただし要望1（無料環境移行）で構成が変わる可能性があるため、手順2-A完了後に実装方法を確定する** |

### 5-3. 新規機能要件ID（セキュリティ由来）

| ID | 名称 | 由来 | 優先度 | 備考 |
|---|---|---|---|---|
| F-17 | ログインのレートリミット | YEL-02 | **高** | 法的前提の担保。手順2-Dで再評価 |
| F-18 | 日付パラメータのバリデーション | YEL-03 | 中 | — |
| F-19 | 非推奨API（`datetime.utcnow()`）の置換 | GRN-01 | 低 | — |
| F-20 | 外部リンクのスキーム検証 | GRN-03 | 中 | F-13と同一ファイルを改修 |
| F-21 | CORS設定の限定 | GRN-04 | 中 | 手順2-A完了後に実装方法を確定 |

> **見送り（⏭️）の記録**: GRN-02は対応しない。理由は上表の通り。**手順7.5の指摘対応表に「⏭️受容（理由）」として記録し、追跡を途切れさせない**（計画書 手順7.5）。

---

## 6. 非機能要件

フェーズ2（`requirements_p2_v0.12.md` §5）を継承する。フェーズ3で追加・変更する項目のみ記載する。

| # | 項目 | 内容 |
|---|---|---|
| 1 | **展開状態の永続化** | しない（メモリのみ）。localStorage・sessionStorage・Cookieを使用しない方針を維持 |
| 2 | **関連ニュースの応答性能** | 展開操作から表示まで体感で待たされないこと（具体的な閾値は手順3で定義） |
| 3 | **可観測性** | §4の運用指標を取得できること。取得手段と記録欄をセットで設計すること |

---

## 7. スコープ外（フェーズ3対象外）

| # | 項目 | 理由 |
|---|---|---|
| 1 | F-09（記事全文翻訳展開） | フェーズ2でスコープアウト確定（著作権法第27条・翻訳権）。**F-13は表示制御であり別物** |
| 2 | C-04（順位タブ） | フェーズ2でスコープアウト確定（BALLDONTLIE 401）。F-12（外部リンク）で代替済み |
| 3 | 知人への公開範囲拡大 | フェーズ2でスコープアウト確定（著作権法第30条のグレーゾーン） |
| 4 | GRN-02（`USERNAME` 変数名の変更） | §5-2の判断により見送り |
| 5 | 展開状態の永続化 | §6-1の方針 |
| 6 | 関連ニュースの無限スクロール・ページング | 5件固定のため不要 |

---

## 8. 手順2への引き継ぎ事項

本書の定義を確定させるために、手順2（リスク事前調査）で確認が必要な事項。

| # | 引き継ぎ先 | 確認事項 | 本書の該当箇所 |
|---|---|---|---|
| 1 | **手順2-A** | Persistent Disk の実使用量（**実測手段の確保が先**） | §4-1 指標8 |
| 2 | 手順2-A | 無料環境移行の可否 → 確定後に要望1・F-21・セキュリティYEL-04を判断 | §1-1・§5-1・§5-3 |
| 3 | **手順2-B** | F-13が著作権上の新たなリスクを生まないか | §2-1-3 |
| 4 | **手順2-B** | F-15の算出方式のコスト（**移行後の無料枠で成立するか**） | §2-3-2 |
| 5 | 手順2-B | F-16のプロンプト追記によるコスト増 | §3-3-3 |
| 6 | **手順2-C** | 監視方式の到達可能性の実証 → 確定後に改修要件5bを定義 | §1-1 |
| 7 | **手順2-D** | YEL-02（レートリミット）を**法的前提を崩すリスク**として再評価 | §5-2 |

---

## 9. 差し戻し確認チェックリスト（フェーズ1レビュー提言1・継承）

本書のレビュー時に確認する項目。

- [ ] 各機能要件に**優先度**と**区分**（追加／変更／バグ修正）が付与されているか
- [ ] 設計判断に**採否と根拠**が記録されているか（不採用案の理由を含む）
- [ ] **境界条件・状態遷移**が明示されているか（F-13の5状態）
- [ ] スコープ外項目に**理由**が記載されているか
- [ ] 手順2への引き継ぎ事項が**引き継ぎ先の手順とともに**列挙されているか
- [ ] 数値・閾値に**根拠**が記述されているか（フェーズ1レビュー提言10）
- [ ] 品質基準が**合否判定項目**と**改善バックログ項目**に分離されているか（振り返り3-5）
- [ ] 運用指標に**取得手段と記録欄**の両方が定義されているか（振り返り3-8）

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v0.1 | 2026-08-24 | 初版作成。①F-13（本文展開表示）を定義し、**F-05との関係を「二段展開・両方存続」と決定**（Takumi様2026-08-24）。状態遷移と5つの境界条件を明示、②F-14（フィルタラベル）を実装確認の上「全チーム／**Spursのみ**」と決定、③F-15（関連ニュース）を**展開時に下部5件**と決定。関連度の定義を本書で行い、算出方式は手順2-Bへ引き継ぎ、④**§3で品質基準を合否判定項目と改善バックログ項目に分離**（振り返り3-5対応）、⑤F-16（表記統一）を**プロンプト方式**と決定し改善バックログ項目に位置づけ、⑥**§4で運用指標9件を定義**（基盤非依存）。取得手段と記録欄のセット設計を明記（振り返り3-8対応）、⑦§5でセキュリティ指摘の対応要否を判断しF-17〜F-21を定義。GRN-02は⏭️見送り（理由を記録）、⑧§8で手順2への引き継ぎ事項7件を引き継ぎ先とともに列挙 |
